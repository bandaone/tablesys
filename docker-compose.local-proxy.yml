version: '3.8'
services:
  local-proxy:
    image: nginx:stable
    container_name: tablesys-local-proxy
    restart: unless-stopped
    network_mode: "host"
    volumes:
      - ./scripts/nginx/local_proxy.conf:/etc/nginx/conf.d/default.conf:ro
      - ./logs/nginx:/var/log/nginx
